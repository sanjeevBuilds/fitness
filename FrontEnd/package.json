{
  "name": "health-fitness-frontend",
  "version": "1.0.0",
  "description": "HealthQuest Frontend - Static HTML/CSS/JS files",
  "scripts": {
    "build": "echo 'Frontend is already built'",
    "start": "npx http-server Public -p 3000 -o && start http://localhost:3000/Home/home.html"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
} 