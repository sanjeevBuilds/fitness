document.getElementById("diet-form").addEventListener("submit", function (e) {
  e.preventDefault();

  // Simulate form handling
  alert("Preferences saved! Heading to Step 4...");

  // Simulate progress bar update (in actual use, replace with navigation logic)
  document.querySelector(".progress").style.width = "100%";
});
